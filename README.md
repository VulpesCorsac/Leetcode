# Leetcode
Some Leetcode tasks

Add this line at the beginning of your solution to speed up (imo only after OK verdict)
```objectivec
static auto _ = [] () {ios_base::sync_with_stdio(false);cin.tie(nullptr);return 0;}();
```